/**
 * Representatin of an item inside of the breadcrumb.
 */
export interface BreadcrumbItem {
  // Name of the route displayed to the user
  name: string;
  // URL that this item represents
  url: string;
}
